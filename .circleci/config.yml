version: 2
jobs:
  build:
    docker:
      - image: "debian:stretch"
    steps:
      - checkout
      - run:
          name: Install G++
          command: apt-get update && apt-get install -y g++ make
      - run:
          name: Build all
          command: make all